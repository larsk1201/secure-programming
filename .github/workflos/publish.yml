name: Publish

on:
  push:
    branches:
      - feat/ci-cd
  release:
    types:
      - published
      - released
      - prereleased
      - edited
  create:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - name: checkout code
        uses: actions/checkout@v6

      - name: Temp test
        run: |
          ls
          ls -al

#      - name: Build android package
#        uses: actions/build-android-app@v1.5.0
#        with:
#          project-path: ~/
#          output-path: ~/guinetrade.apk
#
#      - name: Publish static package
#        if: github.ref_type != 'tag'
#        uses: actions/upload-artifact@v7
#        with:
#          name: guineatrade.apk
#          path: ~/guinetrade.apk
#          if-no-files-found: error
#          retention-days: 30
#          archive: false
#          overwrite: true
#      - name: Generate hashes
#        run: |
#          sha256sum ~/guinetrade.apk >> ~/checksum.txt
#      - name: Release
#        uses: softprops/action-gh-release@v3
#        if: github.ref_type == 'tag'
#        with:
#          files: |
#            ~/guinetrade.apk
#            ~/checksum.txt
#        env:
#          GITHUB_TOKEN: ${{ github.token }}
